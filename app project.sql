create database student
use student

CREATE TABLE Login (
    username VARCHAR(50),
    password VARCHAR(50),
    email VARCHAR(100)
);


CREATE TABLE StudentDetails (
    name VARCHAR(100),
    student_id INT,
    course VARCHAR(100)
);


CREATE TABLE Grade (
    subject_name VARCHAR(100),
    grade VARCHAR(5)
);
select*from Login






